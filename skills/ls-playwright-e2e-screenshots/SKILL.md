---
name: ls-playwright-e2e-screenshots
description: Use when creating E2E Playwright tests or browser automation tests with visual verification. Triggers on 'e2e test', 'playwright test', 'end to end test', 'browser test with screenshots', 'visual verification test'.
---

Create E2E Playwright tests with screenshots. Test one thing per test. Take verification screenshots at each phase. Stop and review those screenshots before running the next test. Use `e2e/screenshots/` for screenshots. Install with `npx playwright install chromium` if needed.
